{
    "name": "classic",
    "version": "1.0.0",
    "dependencies": {
        "codebase": "1.0.0"
    },
    "scripts": {
        "build": "vue-cli-service build --mode production --name rds && cp -ru php dist"
    }
}